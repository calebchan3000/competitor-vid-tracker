{
  "name": "competitor-video-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "description": "Web-based omni-channel YouTube competitor tracker. Fully local: archives Studio audience screenshots as dated snapshots and enriches competitors via the YouTube Data API into a markdown file-DB.",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "tracker": "node scripts/tracker.mjs",
    "list": "node scripts/tracker.mjs list",
    "discord:intake": "node scripts/discord-audience-intake.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
